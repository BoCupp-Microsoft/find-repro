{
  "name": "find-repro",
  "version": "1.0.0",
  "description": "Reusable driver for reproducing errors in the Teams WebView2 host via raw CDP.",
  "private": true,
  "license": "Commercial Confidential",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "find-repro-serve": "./bin/serve.mjs",
    "find-repro-run": "./bin/run.mjs"
  },
  "scripts": {
    "serve": "node ./bin/serve.mjs",
    "run": "node ./bin/run.mjs",
    "test": "node --test --test-concurrency=1 \"test/**/*.test.mjs\""
  }
}
